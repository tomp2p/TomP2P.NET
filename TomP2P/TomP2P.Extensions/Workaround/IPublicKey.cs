namespace TomP2P.Extensions.Workaround
{
    /// <summary>
    /// As there is no .NET equivalent to the java.security.PublicKey interface,
    /// this interface serves as a workaround.
    /// </summary>
    public interface IPublicKey
    {
    }
}
